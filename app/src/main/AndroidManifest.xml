<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.matheus.gotapiindiano">
    <!-- Internet permissão -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.NoActionBar">
        <activity
            android:name=".CasaActivity"
            android:theme="@style/Theme.AppCompat"></activity>
        <activity android:name=".PersonagemActivity" />
        <activity android:name=".InitialActivity" />
        <activity android:name=".AdicionarLivroActivity" />
        <activity android:name=".AdicionarPersonagemActivity" />
        <activity android:name=".AdicionarCasaActivity" />
        <activity android:name=".EditarPersonagemActivity" />
        <activity android:name=".EditarCasaActivity" />
        <activity android:name=".EditarLivroActivity" />
        <activity android:name=".SplashScreenActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
    </application>

</manifest>